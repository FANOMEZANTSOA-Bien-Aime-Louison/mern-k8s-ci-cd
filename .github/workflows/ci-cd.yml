name: CI-CD DOKS

on:
  push:
    branches: [main]

jobs:

  # ========================
  # 1. BUILD & PUSH (CI)
  # ========================
  build:
    runs-on: ubuntu-latest

    outputs:
      image_tag: ${{ steps.vars.outputs.tag }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set image tag
        id: vars
        run: echo "tag=${GITHUB_SHA}" >> $GITHUB_OUTPUT

      - name: Install doctl
        uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      - name: Login registry
        run: doctl registry login

      # Backend image
      - name: Build backend
        run: |
          docker build -t registry.digitalocean.com/${{ vars.REGISTRY_NAME }}/taskflow-backend:${{ steps.vars.outputs.tag }} ./backend

      - name: Push backend
        run: |
          docker push registry.digitalocean.com/${{ vars.REGISTRY_NAME }}/taskflow-backend:${{ steps.vars.outputs.tag }}

      # Frontend image
      - name: Build frontend
        run: |
          docker build -t registry.digitalocean.com/${{ vars.REGISTRY_NAME }}/taskflow-frontend:${{ steps.vars.outputs.tag }} ./frontend

      - name: Push frontend
        run: |
          docker push registry.digitalocean.com/${{ vars.REGISTRY_NAME }}/taskflow-frontend:${{ steps.vars.outputs.tag }}

  # ========================
  # 2. DEPLOY (CD)
  # ========================
  deploy:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install doctl
        uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      - name: Kubeconfig
        run: doctl kubernetes cluster kubeconfig save ${{ vars.CLUSTER_NAME }}

      # Update backend image
      - name: Deploy backend
        run: |
          kubectl set image deployment/taskflow-backend taskflow-backend=registry.digitalocean.com/${{ vars.REGISTRY_NAME }}/taskflow-backend:${{ needs.build.outputs.image_tag }}

      # Update frontend image
      - name: Deploy frontend
        run: |
          kubectl set image deployment/taskflow-frontend taskflow-frontend=registry.digitalocean.com/${{ vars.REGISTRY_NAME }}/taskflow-frontend:${{ needs.build.outputs.image_tag }}
